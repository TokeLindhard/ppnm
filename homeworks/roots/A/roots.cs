using System;
using static System.Math;

public class rootfinder{
    
    public static vector newton(Func<vector,vector>f, vector x0, double eps=1e-2){
        return x;
    }

    public static double delta = 0; //we change this later, when going through the xi's
    public static vector jacobi(Func<vector,vector>f, vector x0){ //finds the partial derivatives using eq. 7
        for(int i=0;i<x0.size;i++){
            delta = Abs(x0[i])*2e-26;
            for(int k=0;i<x0.size;k++){

            }
        }
        return J;
    }
}